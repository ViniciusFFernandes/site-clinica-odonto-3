# Build do CSS

O CSS final (`css/main.css`) é gerado a partir de `build/input.css` com o Tailwind CSS v4.

```bash
npm install
npm run build
```

Edite os textos e dados em `config/clinica.php` — o HTML (`index.php`) não precisa mudar.
